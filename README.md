QUICKSTART:

npm init --yes
npm install --save graphql
npm install --save fast-xml-parser

node fetcher.js


Project about using Met Eireann data to be displayed. (based on some seminar stuff, i might use that data again for some more complex tasks)
